import admin from 'firebase-admin';
import path from 'path';

// Load service account (mocking this as I don't have the cert, but maybe we can use the default or just skip it and check using the client SDK instead)

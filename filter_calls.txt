src/services/courseGrades.bench.test.ts:85:          gradesToReturn = mockGrades.filter((g) => periodFilter.val.includes(g.periodId));
src/services/courseGrades.bench.test.ts:115:    const expectedCount = mockGrades.filter((g) =>
src/services/systemMaintenance.ts:84:    .filter((period) => period.academicYear === latestAcademicYear)
src/services/systemMaintenance.ts:91:  const existingNextPeriods = periods.filter((period) => period.academicYear === nextAcademicYear);
src/components/Attendance/StudentAttendance.tsx:27:        const studentRecords = attendance.filter((a) => a.studentId === studentId);
src/components/Attendance/StudentAttendance.tsx:28:        const present = studentRecords.filter((r) => r.status === 'present').length;
src/components/Attendance/StudentAttendance.tsx:29:        const absent = studentRecords.filter((r) => r.status === 'absent').length;
src/components/Attendance/StudentAttendance.tsx:30:        const late = studentRecords.filter((r) => r.status === 'late').length;
src/components/Attendance/StudentAttendance.tsx:132:                        .filter((record) => record.status !== 'present')
src/components/Attendance/StudentAttendance.tsx:211:                    {records.filter((record) => record.status !== 'present').length === 0 && (
src/components/Attendance/TeacherAttendance.tsx:44:            filtered = filtered.filter((c) => c.teacherId === user.id);
src/components/Attendance/TeacherAttendance.tsx:48:        return filtered.filter((c) => c.dayOfWeek === dayOfWeek);
src/components/Attendance/TeacherAttendance.tsx:56:        return students.filter((s) => (s as Student).classId === selectedCourse.classId);
src/components/UI/FileUpload.tsx:107:    setUploadedFiles((prev) => prev.filter((_, i) => i !== index));
src/components/Layout/Sidebar.tsx:35:  const unreadCount = messages.filter((m) => m.receiverId === user?.id && !m.read).length;
src/components/Layout/NotificationBell.tsx:43:    const unreadMessages = messages.filter((m) => m.receiverId === user?.id && !m.read);
src/components/Layout/NotificationBell.tsx:60:        .filter((g) => g.studentId === user.id)
src/components/Layout/NotificationBell.tsx:83:      .filter((e) => new Date(e.start) > new Date())
src/components/Layout/NotificationBell.tsx:140:    const messageNotifs = notifications.filter((n) => n.type === 'message' && n.originalId);
src/components/Layout/NotificationBell.tsx:146:    const otherNotifIds = notifications.filter((n) => n.type !== 'message').map((n) => n.id);
src/components/Grades/GradeModal.tsx:40:    ? students.filter((s) => (s as Student).classId === classId)
src/components/Grades/TeacherGradesView.tsx:195:              {tg.filteredStudents.map((student) => (
src/components/Grades/TeacherGradesView.tsx:461:        students={tg.filteredStudents}
src/components/Grades/StudentGradesView.tsx:35:    return studentGrades.filter((g) => g.subject === selectedSubject);
src/components/bulletin/BulletinPreview.tsx:34:      .filter((c) => c.studentId === student.id && c.periodId === period.id)
src/components/bulletin/BulletinPreview.tsx:53:      const courseGrades = grades.filter(
src/components/bulletin/BulletinPreview.tsx:56:      return courseGrades.filter((g) => {
src/components/bulletin/BulletinPreview.tsx:83:        courseData.filter((c) => c.average !== null).length
src/components/bulletin/ClassBulletinListModal.tsx:58:    const studentAttendance = attendance.filter((a) => {
src/components/bulletin/ClassBulletinListModal.tsx:64:    const justified = studentAttendance.filter(
src/components/bulletin/ClassBulletinListModal.tsx:67:    const unjustified = studentAttendance.filter(
src/components/bulletin/ClassBulletinListModal.tsx:70:    const late = studentAttendance.filter((a) => a.status === 'late').length;
src/components/Users/UserImportWizard.tsx:278:    const validCount = rows.filter((row) => row.isValid).length;
src/components/Homework/SubmitHomeworkModal.tsx:47:    setExistingFiles(existingFiles.filter((_, i) => i !== index));
src/components/Homework/HomeworkForm.tsx:38:        .filter((c) => !formState.selectedClassId || c.classId === formState.selectedClassId)
src/components/Homework/HomeworkForm.tsx:39:        .filter((c) => userRole !== 'teacher' || c.teacherId === userId)
src/components/Schedule/ExamModal.tsx:158:                  .filter((c) => c.teacherId === teacherId)
src/components/Schedule/ExamModal.tsx:170:            const classCourses = courses.filter((c) => c.classId === classId);
src/components/Schedule/ScheduleDesktop.tsx:158:                                            const startingCourses = courseInfos.filter((ci) => ci.isStart);
src/components/Schedule/ScheduleDesktop.tsx:160:                                            const visibleCourses = startingCourses.filter(
src/components/Schedule/UpcomingEventsModal.tsx:23:      .filter((e) => new Date(e.start) >= new Date())
src/components/Schedule/UpcomingEventsModal.tsx:26:      .filter((h) => new Date(h.dueDate) >= new Date())
src/components/Schedule/HomeworkDetailModal.tsx:99:        {homework.attachments && homework.attachments.filter(isSafeUrl).length > 0 && (
src/components/Schedule/HomeworkDetailModal.tsx:103:              {homework.attachments.filter(isSafeUrl).map((url, index) => (
src/components/Schedule/CourseModal.tsx:179:                  .filter((c) => c.teacherId === teacherId)
src/components/Schedule/CourseModal.tsx:204:                .filter((c) => c.teacherId === teacherId)
src/components/Schedule/CourseModal.tsx:206:                .filter((value, index, self) => self.indexOf(value) === index) // Unique values
src/components/Messages/MessageDetail.tsx:137:                  {selectedMessage.attachments.filter(isSafeUrl).map((url, index) => (
src/components/Messages/MessageList.tsx:79:                        aria-label={t('common.filter')}
src/components/Messages/ComposeModal.tsx:70:    setRecipients((prev) => prev.filter((r) => r !== id));
src/hooks/useHomework.ts:304:    setSelectedFiles(selectedFiles.filter((_, i) => i !== index));
src/hooks/useGradeStats.ts:12:      .filter((g) => g.studentId === studentId)
src/hooks/useGradeStats.ts:20:    const studentAttendance = attendance.filter((a) => a.studentId === studentId);
src/hooks/useGradeStats.ts:29:    const presentCount = studentAttendance.filter((a) => a.status === 'present').length;
src/hooks/useGradeStats.ts:68:      const sGrades = grades.filter((g) => g.studentId === id);
src/hooks/useGradeStats.ts:69:      const sAttendance = attendance.filter((a) => a.studentId === id);
src/hooks/useGradeStats.ts:77:      const presentCount = sAttendance.filter((a) => a.status === 'present').length;
src/hooks/useSchedule.ts:86:      return courses.filter((c) => c.classId === classId);
src/hooks/useSchedule.ts:88:      return courses.filter((c) => c.teacherId === user.id);
src/hooks/useSchedule.ts:96:    return events.filter((e) => {
src/hooks/useSchedule.ts:102:        const teacherClassIds = classes.filter((c) => c.teacherId === user.id).map((c) => c.id);
src/hooks/useSchedule.ts:110:    return homeworks.filter(
src/hooks/useSchedule.ts:124:    return userCourses.filter((course) => {
src/hooks/useSchedule.ts:144:    return userExams.filter((exam) => {
src/hooks/useSchedule.ts:153:    return userHomeworks.filter((hw) => {
src/hooks/useSchedule.ts:166:      .filter((course) => {
src/hooks/useSchedule.ts:187:      .filter((exam) => new Date(exam.start).toDateString() === mobileDate.toDateString())
src/hooks/useSchedule.ts:192:    return userHomeworks.filter(
src/hooks/useSchedule.ts:365:    return weekHomeworks.filter((hw) => {
src/hooks/useBulletinGrades.ts:103:      .filter((c) => c.teacherId === user.id)
src/hooks/useBulletinGrades.ts:106:    return classes.filter((c) => uniqueClassIds.includes(c.id));
src/hooks/useBulletinGrades.ts:112:    return students.filter((s) => (s as Student).classId === selectedClassId);
src/hooks/useBulletinGrades.ts:118:    const teacherCoursesInClass = courses.filter(
src/hooks/useBulletinGrades.ts:175:    const teacherCourses = courses.filter((c) => c.teacherId === user.id);
src/hooks/useBulletinGrades.ts:182:    const relevantGrades = grades.filter(
src/hooks/useBulletinGrades.ts:225:      .filter((item) => item.gradeCount > 0);
src/hooks/useBulletinGrades.ts:303:    const teacherCoursesInClass = courses.filter(
src/hooks/useBulletinGrades.ts:377:    const teacherCourses = courses.filter((c) => c.teacherId === user.id);
src/hooks/useDashboard.ts:59:    return user?.role === 'parent' ? students.filter((s) => s.parentId === user.id) : [];
src/hooks/useDashboard.ts:73:  const teachers = useMemo(() => users.filter((u): u is Teacher => u.role === 'teacher'), [users]);
src/hooks/useDashboard.ts:90:  const allGrades = useMemo(() => grades.filter((g) => g.score > 0), [grades]);
src/hooks/useDashboard.ts:101:    () => messages.filter((m) => m.receiverId === user?.id && !m.read).length,
src/hooks/useDashboard.ts:107:      .filter((e) => new Date(e.start) >= new Date())
src/hooks/useDashboard.ts:122:    () => grades.filter((g) => g.studentId === targetStudentId),
src/hooks/useDashboard.ts:155:      .filter((hw) => {
src/hooks/useTeacherGrades.ts:81:      return classes.filter((c) => c.teacherId === user.id);
src/hooks/useTeacherGrades.ts:93:    return students.filter((s) => (s as Student).classId === selectedClassId);
src/hooks/useTeacherGrades.ts:107:    return classStudents.filter((s) => s.name.toLowerCase().includes(lowerTerm));
src/hooks/useTeacherGrades.ts:117:    const classCourses = courses.filter((c) => c.classId === selectedClassId);
src/hooks/useTeacherGrades.ts:124:      .filter((g) => g.studentId === selectedStudentId)
src/hooks/useTeacherGrades.ts:131:    return grades.filter((g) => g.subject === selectedSubject && classStudentIds.has(g.studentId));
src/hooks/useTeacherGrades.ts:150:    const filteredEvents = events.filter(
src/hooks/useTeacherGrades.ts:158:      .filter(
src/hooks/useTeacherGrades.ts:178:    return ([...filteredEvents, ...filteredHomeworks] as Event[]).sort(
src/utils/userImport.ts:115:  return headers.filter(Boolean);
src/utils/userImport.ts:122:  const matchingHeaders = headers.filter((header) => mapHeaderToField(header) !== null).length;
src/utils/userImport.ts:186:    .filter(Boolean);
src/utils/userImport.ts:274:      .filter((student) => student.role === 'student')
src/utils/userImport.ts:281:      .filter((row) => normalizeRole(row.role) === 'student' && EMAIL_REGEX.test(normalizeEmail(row.email)))
src/utils/userImport.ts:287:    const emails = emailsStr.split(',').map(e => normalizeEmail(e.trim())).filter(Boolean);
src/utils/userImport.ts:337:        const invalidEmails = studentEmails.filter(e => !EMAIL_REGEX.test(e));
src/utils/userImport.ts:338:        const missingEmails = studentEmails.filter(e => !existingStudentEmails.has(e) && !studentEmailsInImport.has(e));
src/utils/userImport.ts:365:  const validRows = rows.filter((row) => row.isValid);
src/utils/userImport.ts:402:  const filteredData = data.filter(row => {
src/utils/userImport.ts:433:  const filteredData = data.filter(row => normalizeRole(row.role) === 'parent');
src/utils/userImport.ts:440:      (row.studentEmail || '').split(',').map((e: string) => e.trim().toLowerCase()).filter(Boolean)
src/utils/userImport.ts:487:    .filter((row) => row.isValid)
src/utils/pdfGenerator.ts:45:      .filter((c) => c.studentId === student.id && c.periodId === period.id)
src/utils/pdfGenerator.ts:64:      const courseGrades = grades.filter(
src/utils/pdfGenerator.ts:67:      return courseGrades.filter((g) => {
src/utils/pdfGenerator.ts:94:        courseData.filter((c) => c.average !== null).length
src/utils/academic.ts:35:  return periods.filter((p) => p.academicYear === targetPeriod!.academicYear).map((p) => p.id);
src/pages/common/Messages.tsx:65:    return messages.filter((msg) => {
src/pages/common/Messages.tsx:92:    const visibleUsers = users.filter((u) => {
src/pages/common/Messages.tsx:146:    return allRecipientOptions.filter((item) =>
src/pages/common/Messages.tsx:209:    setAttachments((prev) => prev.filter((_, i) => i !== index));
src/pages/common/Messages.tsx:229:            const classStudents = users.filter(
src/pages/common/Profile.tsx:28:    const teacherCourses = courses.filter((c) => c.teacherId === user.id);
src/pages/common/Profile.tsx:33:    const teacherClasses = classes.filter(
src/pages/common/Profile.tsx:118:    const children = students.filter((s) => parentData.childrenIds?.includes(s.id));
src/pages/common/Homework.tsx:38:    user.role === 'teacher' ? classes.filter((c) => c.teacherId === user.id) : classes;
src/pages/common/Homework.tsx:40:  const activeHomeworks = homeworks.filter((h) => new Date(h.dueDate) >= now);
src/pages/common/Homework.tsx:41:  const pastHomeworks = homeworks.filter((h) => new Date(h.dueDate) < now);
src/pages/common/Homework.tsx:49:    ? homeworks.filter((h) => h.classId === targetClassId)
src/pages/common/Homework.tsx:51:  const studentActiveHomeworks = studentHomeworks.filter((h) => new Date(h.dueDate) >= now);
src/pages/common/Resources.tsx:103:  const filteredResources = resources.filter((r) => {
src/pages/common/Resources.tsx:180:                {resources.filter((r) => r.type === 'pdf').length}
src/pages/common/Resources.tsx:193:                {resources.filter((r) => r.type === 'image').length}
src/pages/common/Announcements.tsx:133:    .filter((a) => canSeeAnnouncement(a))
src/pages/common/Announcements.tsx:134:    .filter((a) => filter === 'all' || a.type === filter)
src/pages/common/Announcements.tsx:185:              {announcements.filter((a) => a.type === 'urgent').length}
src/pages/common/Announcements.tsx:196:              {announcements.filter((a) => a.type === 'event').length}
src/pages/common/Announcements.tsx:207:              {announcements.filter((a) => a.type === 'info').length}
src/pages/common/Announcements.tsx:215:              {announcements.filter((a) => a.pinned).length}
src/pages/student/StudentBulletin.tsx:31:    return students.filter((s) => parentUser.childrenIds?.includes(s.id));
src/pages/student/StudentBulletin.tsx:64:    return teacherComments.filter((c) => c.periodId === selectedPeriod && c.isValidated);
src/pages/student/StudentBulletin.tsx:70:    return courses.filter((c) => c.classId === (studentData as Student).classId);
src/pages/student/StudentBulletin.tsx:77:    const periodAttendance = attendance.filter((a) => {
src/pages/student/StudentBulletin.tsx:87:      justified: periodAttendance.filter((a) => a.status === 'absent' && a.isJustified).length,
src/pages/student/StudentBulletin.tsx:88:      unjustified: periodAttendance.filter((a) => a.status === 'absent' && !a.isJustified).length,
src/pages/student/StudentBulletin.tsx:89:      late: periodAttendance.filter((a) => a.status === 'late').length,
src/pages/teacher/BulletinGrades.tsx:170:                const studentComments = classComments.filter(
src/pages/teacher/BulletinGrades.tsx:180:                const validatedCount = studentComments.filter((c) => c.isValidated).length;
src/pages/director/BulletinDashboard.tsx:54:      const classStudents = students.filter((s) => (s as Student).classId === cls.id);
src/pages/director/BulletinDashboard.tsx:57:      const classCourses = courses.filter((c) => c.classId === cls.id);
src/pages/director/BulletinDashboard.tsx:87:    const validatedClasses = Object.values(classStats).filter((s) => s.isFullyValidated).length;
src/pages/director/BulletinDashboard.tsx:237:                          {students.filter((s) => (s as Student).classId === classItem.id).length}{' '}
src/pages/director/BulletinDashboard.tsx:317:          students={students.filter((s) => (s as Student).classId === viewingClassId)}
src/pages/director/BulletinDashboard.tsx:318:          courses={courses.filter((c) => c.classId === viewingClassId)}
src/pages/director/Classes.tsx:33:  const teachers = users.filter((entry) => entry.role === 'teacher');
src/pages/director/Classes.tsx:70:    students.filter((student) => (student as Student).classId === classId);
src/pages/director/Classes.tsx:101:    return students.filter((entry) => {
src/pages/admin/Users.tsx:41:    () => (isSuperadmin ? users : users.filter((u) => u.role !== 'superadmin')),
src/pages/admin/Users.tsx:47:      visibleUsers.filter((u) => {
src/pages/admin/Users.tsx:332:          .filter((r) => currentUser?.role === 'superadmin' || r !== 'superadmin')
src/pages/admin/Users.tsx:533:                            setSelectedStudentIds(prev => prev.filter(id => id !== student.id));
src/context/slices/UserContext.tsx:106:  const students = useMemo(() => users.filter((u): u is Student => u.role === 'student'), [users]);
